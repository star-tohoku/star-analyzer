# 解析ノート 2026-09-07

**解析名:** AuAu13p5 Λ／anti-Λ full KFParticle reconstruction  
**データ:** STAR Run20 AuAu13p5 fixed-target、P24iy PicoDst  
**作業者:** yichikawa / Codex  
**記録時点:** 2026-09-07 05:55 EDT以降の終了整理  
**状態:** ユーザー指示により実装・検証を中断し、日報だけを保存。全工程完了ではない。

---

## 1. 明日の再開点（最優先）

最後のユーザー指示:

> 既存の解析コードを見てもらいたいですが、config/analysisのyamlファイルで、コライダーモードか、fixed target modeかでvertex cutの位置を変えています。それを適応して欲しいです。

**次回は、既存解析が `config/analysis/analysis_info_*.yaml` のモードからvertex cut位置を切り替える仕組みを確認し、同じ規約をKF解析へ適用するところから再開する。**

- 今日の13p5実行では、原点中心の半径カットが大半のイベントを除外した。
- 当初はxwu2参照コードの中心 `(-0.4, -2.0) cm` を新KF設定へ明記する案を検討し、ユーザーから中心修正・再検証の了承を得た。
- **その後の最新指示は、既存のcollider／fixed-targetモード切替を利用すること。固定値の直接追加を最終方針としない。**
- 終了時に実ファイルを再確認したところ、`event_auau13p5_anaLambda_KFParticle.yaml` に `vtxCenterX/Y` はまだ追加されていない。参照中心への変更・変更後の再実行は未実施。
- 新しい `analysis_info_auau13p5_anaLambda_KFParticle.yaml` には `analysis.mode: "fxtmult"` がある。既存切替で認識されるキー・値、適用箇所、event YAMLとの優先順位を次回確認する。ここではまだ正しい連携ができたと扱わない。
- `StLambda`／`StLambdaMaker`／既存Helix解析ソース・設定は変更しない。

## 2. 今日実装したこと

承認済み計画: [full reconstruction計画](../../mdfiles/plan_kfparticle_lambda_full_reconstruction.md)。ユーザー指定のクラス説明も保持した。

### Full coreと既存STARとの分離

- scalar手動p×π fitから、`StPicoKFParticleInterface → KFParticleTopoReconstructor → KFParticleFinder` によるイベント単位再構成へ移行。
- 同じ `.DEV2/StRoot/KFParticle` snapshotから39ファイル／11実装を取り込んだ。
- `KFVertex`、`KFPTrackVector`、`KFPEmcCluster`、`KFParticleSIMD`、`KFParticlePVReconstructor`、`KFParticleFinder`、`KFParticleTopoReconstructor`を含む。
- 既存 `StarRoot` の旧グローバルKFParticleとのABI衝突を避けるため、`star_analyzer_kfp` namespaceへ隔離。include guardも分離した。
- `tools/kfparticle/import_core.py`、`SNAPSHOT.json`、`KFParticleSnapshot.h`で再生成方法・SHA256・取得元を記録。
- core source ID: `033c38b7128ea7cb351a2785a3a65ff0ec2e19e391ca3680d8ffd4d17c27ceb5`。
- 互換patchはnamespace等の機械的変換と、`KFPTrack`のstandalone時のROOT reflection無効化。coreの数式・探索・内部cutは変更していない。
- ROOT 6は導入せず、SL24y／ROOT 5のまま実イベントまで動作した。

### Pico adapter／Maker／設定

- SL24yのPico covarianceから6成分Cartesian parameter・21成分covarianceを構築。
- 参照InterfaceのTPC `dEdxPull`、soft／strict TOF、複数PID仮説、primary分類、PV入力、SortTracks、Finder呼出しを接続。
- TOF index 0を有効扱い。欠落・不正index、covariance、非連続／重複track ID、イベント境界・磁場切替を検査。
- Maker側の旧scalar pair loopは削除。暗黙のscalar fallbackや、旧daughter Helix DCA cutの持ち込みはない。
- raw parent massと、別コピーにPV拘束を適用したtopology／decay-length量を区別。
- `KfLambdaCandidates` tree、符号別raw／selected mass histogram、段階別 `hKfStages`、PID／fit QA、設定・backend fingerprint・`KFRunStatus`を出力。
- KF設定はschema 2。`backend: finder_topo`を必須とし、不明・重複・旧scalarキー・型不正・不正値を拒否。
- KF Makerが従来は `maxNTr` しか適用していなかった点を修正し、event YAMLのVz、Vr／中心、refMult、valid VPDの差cutも適用。ただし、**analysis-infoのモード切替への接続は上記の未完了事項**。
- `config/mainconf/main_auau13p5_anaLambda_KFParticle.yaml` と参照先10個の専用YAMLを新規作成。既存13p5設定は変更していない。
- 13p5 TOF係数は `xwu2_lambda_reference_unvalidated_auau13p5` と明示。Run20への校正妥当性・参照と同じPID効率を保証しない。

### 実行の安全性

- 入力／必要branch／リストから省略されたファイル／0イベントを検査し、失敗を明示。
- 既存出力ファイルを上書きしない `CREATE` 方式。途中出力を成功と扱わない。
- ACLiCはプロセス別 `tmp/kf-aclic/` ディレクトリで生成し、並列cache競合を回避。
- ROOT 5が致命的signalを回復して終了コード0を返すことへの対策をKF runnerに限定して追加。
- `singularity_make.sh` はKFテスト専用にruntime library pathを引き継ぐだけの限定変更。既存build用LD設定は維持した。

## 3. 主な変更ファイル

- `StRoot/KFParticle/`、`tools/kfparticle/import_core.py`
- `StMaker/kfparticle/KfParticleHelper.{h,cxx}`
- `StMaker/kfparticle/StPicoKFParticleInterface.{h,cxx}`、同README
- `StMaker/StLambdaKFParticleMaker/StLambdaKFParticleMaker.{h,cxx}`
- `analysis/anaLambda_KFParticle.C`、`analysis/run_anaLambda_KFParticle.C`
- `include/cuts/KfParticleCutConfig.h`、`src/cuts/KfParticleCutConfig.cpp`
- `Makefile`、`script/singularity_make.sh`、`script/singularity_run_anaLambda_KFParticle.sh`
- `docs/REFERENCE.md`、`mdfiles/plan_kfparticle_lambda_full_reconstruction.md`
- `tests/kfparticle_full_chain.cxx`、`tests/kfparticle_pico_adapter.cxx`
- `tests/bootstrap_kfparticle_tests.C`、`tests/run_kfparticle_tests.C`
- `tests/check_kfparticle_output.C`、`tests/inspect_kf_input_events.C`、`tests/probe_std_edx.C`

13p5の新規設定（すべて `config/` 配下）:

- `mainconf/main_auau13p5_anaLambda_KFParticle.yaml`
- `analysis/analysis_info_auau13p5_anaLambda_KFParticle.yaml`
- `cuts/event/event_auau13p5_anaLambda_KFParticle.yaml`
- `cuts/track/track_auau13p5_anaLambda_KFParticle.yaml`
- `cuts/pid/pid_auau13p5_anaLambda_KFParticle.yaml`
- `cuts/v0reco/v0_auau13p5_anaLambda_KFParticle.yaml`
- `cuts/mixing/mixing_auau13p5_anaLambda_KFParticle.yaml`
- `cuts/centrality/centrality_auau13p5_anaLambda_KFParticle.yaml`
- `maker/maker_auau13p5_anaLambda_KFParticle.yaml`
- `cuts/kf/kf_auau13p5_anaLambda_KFParticle.yaml`
- `hist/hist_auau13p5_anaLambda_KFParticle.yaml`

AuAu19 KF cut／hist設定をschema 2へ更新し、`cuts/kf/kf_xwu2_reference_anaLambda_KFParticle.yaml`も用意した。AuAu19のHelix設定は未変更。

## 4. Buildと自動テスト

環境: STAR `SL24y`、`sl73_x8664_gcc485`、ROOT `5.34/38`、GCC `4.8.5`、batch相当Singularity。

KF専用flags: `-std=c++11 -msse4.1 -D__ROOT__ -DKFParticleStandalone -DHomogeneousField=`。

- full core／adapter／Makerのcompile・link、KF解析macroのACLiC、全ライブラリロード: 成功。
- `all: core kfparticle-analysis`を維持。既存coreにKF依存を追加していない。
- 今日のclean複合コマンド:

```bash
./script/singularity_make.sh config/mainconf/main_auau13p5_anaLambda_KFParticle.yaml all test-kfparticle-full-chain test-kfparticle-pico-adapter KF_TEST_CUTS=config/cuts/kf/kf_auau13p5_anaLambda_KFParticle.yaml -s -j4
```

  yaml-cppを含め生成物をcleanしてライブラリを再構築した。ただし**このコマンド全体の終了コードは2**。後述のfull-chainテストのassertionが原因。保存した [cleanログ](kfparticle_logs/clean_all_tests.log) を全成功ログと読み替えない。

- Pico adapter人工統合テスト: **PASS／status 0**。
  - 6 parameter／21 covariance照合、Λ／anti-Λ、非連続ID、複数PID、reset。
  - TOF index 0、beta量子化／欠測、不正参照。
  - dEdxPullと直接Evalの数値対応、保存nSigmaへの暗黙fallbackなし。
  - 不明／重複／旧キー／schema／型／TOF係数次元の否定テスト。
  - 人工Λのraw massは1.122 GeV。親PDG massへ固定していない。
- full-chain人工テスト: assertionの前提を修正後、**個別再実行でPASS／ROOT status 0／make exit 0**（作業セッションで確認。個別成功stdoutは上記cleanログには含まれない）。
  - Λ（Bz=+5）／anti-Λ（Bz=-5）、raw mass 1.12068 ± 0.000621249 GeV。
  - signed whitelist、ID対応／重複排除、empty／no-PV reset、scalar／SIMD磁場。
  - PV・mass拘束コピーを操作しても、raw全parameter／covarianceは不変。
- 次回、最終状態で単独の通常clean `make all` と両テストの通しログを取り直す。今日の結果を「最終版clean all＋全テストが一回でexit 0」とは記述しない。

### 対応したROOT 5／テスト上の問題

1. installed `StarRoot`のROOT依存を明示リンク。さらに同ライブラリには既存の未定義 `THelix3d::operator=` があり、通常のSTAR ROOT loaderと同じ共有module経路でテストする方式にした。偽実装やengine書換えはしていない。
2. CINTは関数内typedef／取得した関数pointer呼出しに制限があるため、テストもbootstrap→ACLiC済runner→compiled fixtureという構成へ変更。
3. exact mass constraint後の分散が丸めで `gᵀCg = -2.1229e-13` になり、正しいmassでも `GetMass` がstatus 1／error 1000を返した。テストをmass／状態／ゼロ分散の検査に改めた（roundoff bound `6.26021e-11`）。**物理engineは変更せず、raw massの正の誤差条件も緩めていない。**
4. 否定テストの意図したERROR表示と、テスト全体の終了statusは区別する。

## 5. 実イベント解析（Analyses run）

### 入力

元リスト: `config/picoDstList/auau13p5GeV.list`（42ファイル、リスト自体は未変更）。

検証したのは、その**先頭1ファイル**:

```text
root://xrdstar.rcf.bnl.gov:1095//home/starlib/home/starreco/reco/production_13p5GeV_fixedTarget_2020/ReversedFullField/P24iy/2020/033/21033026/st_physics_adc_21033026_raw_6500002.picoDst.root
```

ROOT 5の `Netx` pluginがないため、直接remote入力は0イベント／exit 2。ホスト `xrdfs stat` で読取可能・69,953,736 bytesを確認し、`xrdcp`で次へ取得した:

`/tmp/star-kf-validation-20260907.Ho7eLz/auau13p5_first.picoDst.root`

ファイル内1479イベントのうち先頭1000を検証。`TrackCovMatrix`を含む必須branchは存在した。全42ファイルを処理したわけではない。/tmpは永続保存を保証しないため、次回存在確認が必要。

### 成功したKF実行

- mainconf: `config/mainconf/main_auau13p5_anaLambda_KFParticle.yaml`
- jobid: `validation13p5context`（local testラベル、farm jobidではない）
- configlog: **なし**（farm未投入）。KF実効設定はROOT内 `KFParticleEffectiveConfiguration` と [実行ログ](kfparticle_logs/local_smoke_1000_context.log) に保存。
- output: [local_smoke_1000_context.root](../../rootfile/auau13p5_anaLambda_KFParticle_validation_20260907/local_smoke_1000_context.root)
- ROOT metadata: `KFRunStatus=completed`、終了コード0。
- [出力QA](kfparticle_logs/output_qa_1000.log): status、tree／hist／段階counter、ID、有限値、正の誤差の整合性PASS。

| 段階 | 件数 |
| --- | ---: |
| 読み取りイベント | 1000 |
| イベント／centrality選別後のKF処理 | 12 |
| そのイベント内の入力track | 1015 |
| track quality通過 | 322 |
| covariance有効 | 321 |
| PID track | 211 |
| PID仮説 | 394 |
| primary仮説 | 70 |
| Topo particle slots（入力等を含む） | 462 |
| TopoのΛ候補 | 2 |
| candidate検査で不採用 | 1 |
| 有効raw候補（tree） | 1 |
| 最終Λ | 1 |
| 最終anti-Λ | 0 |

有効候補のmass: **1.12202 ± 0.00303336 GeV/c²**。Topoの2個がそのままtreeに2行あるわけではない。候補1個なので**mass peak、効率、物理yield、anti-Λ実データ検証は未達**。

再現用コマンド（出力は必ず未使用の名前にする。明日はモード対応を先に行う）:

```bash
./script/singularity_run_anaLambda_KFParticle.sh /tmp/star-kf-validation-20260907.Ho7eLz/auau13p5_first.picoDst.root rootfile/auau13p5_anaLambda_KFParticle_validation_20260907/NEW_RESULT.root validation13p5 1000 config/mainconf/main_auau13p5_anaLambda_KFParticle.yaml
```

### イベント数が12まで減った理由

[Event-only診断](kfparticle_logs/event_acceptance_1000_runtime.log)を実施し、設定は変更していない。

- 全1000イベントのVzは198.421–201.669 cmで、198–202 cmを通過。
- PV平均: `(x,y)=(-0.225129,-2.28099) cm`。
- 原点から `Vr <= 2 cm` が982イベントを除外し、18イベントだけが残った。
- 残った18ではrefMult／VPD cutによる追加減少なし。VPD差の独立失敗3件はVrで既に除外されていた。
- 18とKF処理12の差6はevent-only診断の対象外であるbad-run／pileup／centrality段階。内訳は未確定。
- 主因はVPDではなく**頂点cutの中心**。最新ユーザー指示に従ってモード連携を次回修正する。
- xwu2参考: `StKFParticleInterface.cxx:289–290` の `IsGoodPV` は中心(-0.4,-2.0)、半径2 cm。これは参考として確認した値であり、既存frameworkのモード規約を置換する根拠にはしない。

### 既存Helixの動作確認

- mainconf: `config/mainconf/main_auau13p5_anaLambda.yaml`
- jobid: `helixcontrol20`
- configlog: なし（local test、既存設定未変更）。
- 同じ入力の先頭20イベントを正常処理し、Init／ACLiC／Finish、exit 0を確認。
- centrality summary: ok 17、invalidCent 3、badRun／pileup／binRejected 0。
- output: [helix_control_20.root](../../rootfile/auau13p5_anaLambda_KFParticle_validation_20260907/helix_control_20.root)
- [実行ログ](kfparticle_logs/helix_control_20.log)
- 先に行った1000イベント版は180秒の制限でtimeout（124）。20イベント成功と混同しない。
- 変更前後の十分な統計でのhistogram数値比較は未実施。

## 6. dEdx初期化クラッシュの原因と修正

最初のlocal KF実行は17イベント目付近で `StdEdxModel` 初期化に入り、`const nknots` 警告後にsegfault。ROOTの回復処理により当時はshell exit 0となったが、**これは失敗実行**である。

原因をSTAR＋StBichselだけの最小probeで切り分けた:

```cpp
// mode 6: nested CINTへのconst初期化文脈の影響を再現（exit 139）
const Long_t result = gROOT->ProcessLine(...);

// mode 7: 成功（exit 0）
Long_t result = 0;
result = gROOT->ProcessLine(...);
```

- [mode 6の失敗ログ](kfparticle_logs/probe_std_edx_6.log)、[mode 7の成功ログ](kfparticle_logs/probe_std_edx_7.log)。
- runnerの宣言と呼出しを分離した後、実13p5の1000イベントも成功。
- テーブルの14 knots、モデルscale 52 eV、PID計算式は変更していない。
- ROOT 6導入、STARインストールの修正、校正tableの差替え、保存nSigmaへのfallbackは不要だった。
- `tests/probe_std_edx.C(6)` は意図的にクラッシュする診断用。通常の成功テストとして実行しない。

## 7. 既存解析保護・git状態

- 今日のgit historyにcommitなし。今回もcommit／push／farm submitはしていない。
- `StLambdaMaker`、旧macro／runner、共通Helix再構成のSHA256は作業前と一致。
- `libStLambdaMaker.so`、`libStCommon.so`、`libStarAnaConfig.so`の `DT_NEEDED` にKF依存なし。ROOT依存は `.so.5`。
- 新coreの定義済みnamespace symbolは357件、調査対象のglobal KF symbolは0件。
- `python3 tools/kfparticle/import_core.py --check`: 39ファイル／11実装の整合性PASS。
- `git diff --check`、変更したshell scriptの `bash -n`: エラーなし。
- 既存の無関係なmodified／untrackedファイルは保持。`tools/auau3p85GeV/`等のユーザー変更を今回の成果と取り違えない。
- `config/`、`tests/`、`analysisnote/`等にはgitignore対象が含まれる。workspaceには保存されているが、通常の `git status` だけでは全新規ファイルが見えない。勝手にignore方針やindexを変更しない。
- cleanで削除したのは再生成可能なobject／共有library／yaml-cpp build生成物で、再構築済み。既存解析ROOT出力やユーザーsourceは削除していない。

## 8. 再開後の順序と残課題

1. 本日報§1の最新指示を確認。既存解析と `config/analysis` のcollider／FXTモード切替実装を調査する。
2. KFの設定読込／セットアップを既存規約へ接続。キーや値を推測して固定値だけを追加しない。既存解析の挙動・設定は保護する。
3. 13p5の**実効**vertex中心をログ／QAで確認し、同じ1000イベントで再検証する。必要なら先頭ファイル全1479イベントへ拡大。
4. 通常clean `make all` と両自動テストの最終通し実行を保存。C++変更時はROOT 5で再buildする。
5. 変更後出力に対して `tests/check_kfparticle_output.C` を実行。失敗・途中出力を解析入力にしない。
6. 十分な実統計でmass分布、daughter PID／TOF、topology量を確認。TOF校正の妥当性、同core・同入力での参照Interface比較、既存Helixの数値非退行は引き続き検証が必要。
7. `mdfiles/plan_kfparticle_lambda_full_reconstruction.md` §9には実装状況の追記があるが、リンク先 `mdfiles/kfparticle_full_implementation_20260907.md` は終了時点で未作成。明日、日報との参照整理を行う（今日はログ以外を編集しない）。

本日報に付属する `kfparticle_logs/` は既存stdoutを保存したもの。元ログとlocal PicoDstは `/tmp/star-kf-validation-20260907.Ho7eLz/` にある。終了整理では新しい解析／buildを実行していない。担当agentの作業を停止し、確認範囲に当方のKF／make／xrdcpプロセスは残っていなかった。他の解析ROOTプロセスには手を触れていない。


---

## Session update — 22:33 EDT / 2026-09-07 — KF mode対応を再開

ユーザーの「復帰しました。作業を再開してください。」を受け、上記§1の再開点を実装・再検証した。
**上記の終了時点の記録はそのまま保持し、ここからが再開後の追加結果。**

- 既存mode切替はgeneratorによるVzのみで、XY中心の自動切替ではなかった。
  旧StLambdaMakerのevent cutはmaxNTrのみだった点も確認したが、既存解析は変更しない。
- 新KF専用KfEventSelectionがanalysis.mode=refmult／fxtmultからevent.vertexByModeを選択。
  FXTはVz[198,202]、中心(-0.4,-2.0)、半径2 cm。値はKF専用YAMLに置き、共有singletonへ書き戻さない。
- 実効設定metadata、排他的event棄却counter、vertex QAを追加。両mode・不正設定の人工テストを追加。
- **通常clean make all: exit 0。full-chain／Pico adapter（mode tests含む）: exit 0。**
- 同じ1000イベント: KF処理12→957。Vr棄却は982→1、VPD差3、centrality invalid39。
  最終Λ3189／anti-Λ463、出力QA PASS。
- 先頭ファイル全1479イベント: event cut通過1475、KF処理1422、Λ4701／anti-Λ716、解析・出力QA exit 0。
  Vr1／VPD3／centrality invalid53、KF error0。
- 旧Helixの同じ20イベントは46ヒストグラム・2,469,952セルが前回出力と完全一致（比較exit 0）。
  保護対象7ファイルSHA256一致、旧共有ライブラリにKF依存なし。
- 新QAによる旧metadata欠落出力の拒否（exit6）、legacy Event診断による階層profile拒否（exit1）も確認。
- commit／push／farm submitなし。既存無関係な変更を保持した。

Analyses run:

- KF mainconf: `config/mainconf/main_auau13p5_anaLambda_KFParticle.yaml`
- local jobid: `validation13p5mode`（1000）、`validation13p5mode1479`（1479）
- ROOT: `rootfile/auau13p5_anaLambda_KFParticle_validation_20260907/local_mode_1000.root`、`local_mode_1479.root`
- Helix mainconf: `config/mainconf/main_auau13p5_anaLambda.yaml`、local jobid `helixmodecontrol20`
- farm configlog: **なし**。KF／eventの実効値は出力ROOT metadataと実行ログに保存。
- 入力は前回と同じ42ファイルリストの先頭1ファイルlocal copy。新規download／全42ファイル実行ではない。

詳細・全コマンド・残課題: [実装・検証記録](../../mdfiles/kfparticle_full_implementation_20260907.md)。
今回のログは [kfparticle_resume_logs/](kfparticle_resume_logs/) に保存し、上記前半のkfparticle_logsは変更していない。
計画§9の未作成だったリンク先も本実装記録として作成し、§10へモード対応を追記した。

次の検証対象は全42ファイル、Run20 PID／TOF校正、独立参照Interfaceとの同一条件数値比較と質量／効率の物理検証。
モード修正・限定実イベント検証の成功と、これらの全面完了は区別する。
