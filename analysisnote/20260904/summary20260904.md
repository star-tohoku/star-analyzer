# 解析ノート 2026-09-04

**解析名:** Au+Au 19 GeV Lambda KFParticle reconstruction
**データ:** STAR PicoDst（実データevent処理は未実施）
**作業者:** yichikawa / Codex

---

## 1. 本日の目的

既存のhelix-based `StLambdaMaker`解析を壊さず、KFParticleを用いた
Lambdaおよびanti-Lambda再構成を独立した解析経路として追加する。
また、通常の `make all` で新旧両方をビルドできるようにする。

## 2. 計画と非回帰方針

- 実装計画を `mdfiles/plan_kfparticle_lambda.md` に整理し、実装・検証状況を追記した。
- `StLambdaMaker`を置換せず、新規`StLambdaKFParticleMaker`を作る方針とした。
- 以下の既存解析ファイルを保護対象とし、最終確認でzero diffを確認した。
  - `StMaker/StLambdaMaker/`
  - `analysis/anaLambda.C`
  - `analysis/run_anaLambda.C`
  - `script/run_anaLambda.sh`
- `make core`はKFParticle非依存、通常の`make all`は既存stackとKFParticle stackの
  両方をビルドする構成とした。

## 3. 実装内容

### KFParticle本体

- STAR `.DEV2/StRoot/KFParticle`から、Lambdaのscalar two-daughter fitに必要な
  最小構成を`StRoot/KFParticle/`へvendorした。
- `KFParticleStandalone`、`HomogeneousField`、`-msse4.1`でコンパイルする
  `libKFParticle.so`を追加した。
- コピー元、対象ファイル、build定義、checksum、licenseを
  `StRoot/KFParticle/PROVENANCE.md`へ記録した。

### PicoDst adapterとLambda Maker

- `StMaker/kfparticle/KfParticleHelper.{h,cxx}`を追加した。
- `StPicoTrackCovMatrix`のhelix parameter、sigma、correlationから
  `StDcaGeometry`を構築し、`GetXYZ()`でKFPTrack用のCartesian parameterと
  21要素covarianceへ変換する処理を実装した。
- SL24yで通常公開されない`StPicoTrackCovMatrix::dcaGeometry()`を使わず、
  globalな`__TFG__VERSION__`も定義していない。
- 新規`StLambdaKFParticleMaker`でproton-pion pairing、Lambda/anti-Lambdaの
  KF fit、mass・topology cut、QA histogram、candidate accessorを実装した。
- KF helperは`libStKfParticleCommon.so`へ分離し、既存`libStCommon.so`へ
  KF依存を混入させていない。

### 設定・実行経路

- optionalな`kf:` mainconf keyと`KfParticleCutConfig`を追加した。
- KF専用mainconf、cut、histogram、analysis-info YAMLを追加した。
- `analysis/anaLambda_KFParticle.C`、`analysis/run_anaLambda_KFParticle.C`、
  通常・Singularity用run scriptを追加した。
- KF macroではPicoDstの`TrackCovMatrix` branchを明示的に有効化した。
- `job/joblist/joblist_auau19_anaLambda_KFParticle.xml`を生成した。
- analysis-infoとjob listの`workDir`を現在のworkspace
  `/star/u/yichikawa/pwg/git_STAR_3/star-analyzer`へ合わせた。
- `docs/REFERENCE.md`へbuild・run方法を追記した。

## 4. Development / git

- 本日作成されたgit commit: なし。
- 作業treeには今回の変更以外にも既存のmodified/untracked filesがあるため、
  それらには触れず保存した。
- `config/`の解析固有YAMLと生成job listはrepository既定の`.gitignore`対象。
  workspace上には存在するが、commit対象にする場合は運用方針の確認が必要。
- `git diff --check`: errorなし。

## 5. Buildおよび依存関係の検証

使用環境:

- STAR library tag: `SL24y`
- ROOT: `5.34/38`
- STAR host system: `sl73_x8664_gcc485`
- wrapper: `script/singularity_make.sh`

結果:

1. clean `make core`: 成功。
   - `libKFParticle.so`、`libStKfParticleCommon.so`、
     `libStLambdaKFParticleMaker.so`が生成されないことを確認。
2. clean ordinary `make all`: 成功。
   - 上記3つのKFライブラリを含め、既存Makerと新規Makerを一括buildできた。
3. `readelf -d`による確認:
   - `libStCommon.so`、`libStLambdaMaker.so`、`libStPhiMaker.so`に
     KFParticleの`DT_NEEDED`なし。
   - `libStLambdaKFParticleMaker.so`のみ
     `libStKfParticleCommon.so`と`libKFParticle.so`を要求。
4. KF専用histogram YAMLに、Makerがfillする全histogram名が存在することを確認。

## 6. Analyses run

### KFParticle smoke test

- mainconf: `config/mainconf/main_auau19_anaLambda_KFParticle.yaml`
- jobid: `kf-smoke`
- command: `script/singularity_run_anaLambda_KFParticle.sh`
- requested events: 1
- configlog: なし（ローカルSingularity smoke test。SUMS submissionではない）
- 結果:
  - KFライブラリload成功。
  - `anaLambda_KFParticle.C`のACLiC compile成功。
  - mainconf読込、StChain `Init()`、`Finish()`、process exit 0。
  - `TrackCovMatrix` branchが有効になったことを確認。
  - 入力ファイル実体が無く、processed eventsは0。

### 既存Lambda regression smoke test

- mainconf: `config/mainconf/main_auau19_anaLambda.yaml`
- jobid: `existing-regression`
- command: `script/singularity_run_anaLambda.sh`
- requested events: 1
- configlog: なし（ローカルSingularity smoke test。SUMS submissionではない）
- 結果:
  - `kf:`を持たない従来mainconfを正常に読込。
  - 既存`anaLambda.C`のACLiC compile、StChain `Init()`、`Finish()`、process exit 0。
  - KFParticleライブラリをロードしない既存経路が維持された。
  - 入力ファイル実体が無く、processed eventsは0。

`job/run/configlog/`には本日作成されたsubmission configlogは無かった。

## 7. 制限事項と次回作業

`config/picoDstList/test.list`が参照する`/home/starlib/...`のPicoDstは、
今回のvalidation nodeには存在しなかった。そのため、実イベントを用いた以下の
physics validationは未完了。

- Lambdaおよびanti-Lambda candidateの実生成
- invariant-mass peakとsidebandの確認
- covariance rejection、fit chi2/NDF、decay-length significanceの分布確認
- NaN/Infが無いことのevent-level確認
- 同一入力eventに対するhelix解析とKFParticle解析の比較
- event count、candidate count、mass、kinematics、decay length、topologyの比較

次回はアクセス可能なPicoDst fileまたはlistを指定し、少数eventで両解析を実行する。
その後、同一入力で出力ROOT fileを比較してKF cutを調整する。

## 8. 再開時の主要ファイル

- `mdfiles/plan_kfparticle_lambda.md`
- `StMaker/StLambdaKFParticleMaker/StLambdaKFParticleMaker.cxx`
- `StMaker/kfparticle/KfParticleHelper.cxx`
- `StRoot/KFParticle/PROVENANCE.md`
- `analysis/anaLambda_KFParticle.C`
- `analysis/run_anaLambda_KFParticle.C`
- `config/mainconf/main_auau19_anaLambda_KFParticle.yaml`
- `config/cuts/kf/kf_auau19_anaLambda_KFParticle.yaml`
- `config/hist/hist_auau19_anaLambda_KFParticle.yaml`
- `job/joblist/joblist_auau19_anaLambda_KFParticle.xml`
