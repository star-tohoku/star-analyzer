# Project rules wrapper

This file is a Cursor wrapper. Canonical naming and layout rules live in:

- [docs/ai/AGENT_RULES.md](../../docs/ai/AGENT_RULES.md)
- [PHILOSOPHY.md](../../PHILOSOPHY.md)

Operational highlights:

- Keep `anaName`, `jobid`, `mainconf`, and macro-base naming consistent.
- Keep paths and output naming tied to the selected `mainconf`.
- Preserve repository layout conventions (`analysis/`, `StMaker/`, `config/`, `script/`, `job/run/`).
- Keep submit output and config snapshots under `job/run/configlog/`.
